#include "sampler.h"

int sampler_init(CSOUND* csound, sampler* obj) {
    (void) csound;
    (void) obj;

    return OK;
}

int sampler_vector(CSOUND* csound, sampler* obj) {
    (void) csound;
    (void) obj;

    return OK;
}
